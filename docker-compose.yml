name: test-preflight # You can also change this to another name to avoid warning with existing group.
services:
  frontend:
    container_name: test-pf-frontend
    build: .
    ports:
      - ${NGINX_PORT}:6000
    networks:
      - test-preflight_pf-net
    environment:
      - NGINX_PROXY=${NGINX_PROXY}
networks:
  test-preflight_pf-net: # Check by using `docker network ls` command
    external: true